{
  "name": "my-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup": "yarn init && yarn add -D @types/node ts-node-dev typescript",
    "setup-with-npm": "npm init && npm i -D @types/node ts-node-dev typescript",
    "start": "ts-node-dev src/index.ts",
    "start2": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4"
  }
}
